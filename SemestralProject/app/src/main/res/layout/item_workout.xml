<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginStart="16dp"
    android:layout_marginEnd="16dp"
    android:layout_marginTop="8dp"
    android:layout_marginBottom="8dp"
    app:cardCornerRadius="16dp"
    app:cardElevation="2dp"
    app:strokeWidth="0dp"
    app:cardBackgroundColor="@color/md_theme_light_surface">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="16dp">

        <!-- Workout Type Icon -->
        <com.google.android.material.card.MaterialCardView
            android:id="@+id/cardWorkoutIcon"
            android:layout_width="48dp"
            android:layout_height="48dp"
            app:cardCornerRadius="24dp"
            app:cardElevation="0dp"
            app:cardBackgroundColor="@color/md_theme_light_primaryContainer"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent">

            <TextView
                android:id="@+id/tvWorkoutIcon"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:gravity="center"
                android:text="🏃"
                android:textSize="24sp" />
        </com.google.android.material.card.MaterialCardView>

        <!-- Hidden image thumbnail -->
        <com.google.android.material.card.MaterialCardView
            android:id="@+id/cardImageThumb"
            android:layout_width="64dp"
            android:layout_height="64dp"
            app:cardCornerRadius="12dp"
            app:cardElevation="0dp"
            app:strokeWidth="1dp"
            app:strokeColor="@color/md_theme_light_outlineVariant"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            android:visibility="gone">

            <ImageView
                android:id="@+id/ivWorkoutThumb"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:scaleType="centerCrop" />
        </com.google.android.material.card.MaterialCardView>

        <!-- Workout Name -->
        <TextView
            android:id="@+id/tvWorkoutName"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:text="Název tréninku"
            android:textSize="16sp"
            android:textStyle="bold"
            android:textColor="@color/md_theme_light_onSurface"
            android:fontFamily="sans-serif-medium"
            android:maxLines="1"
            android:ellipsize="end"
            android:layout_marginStart="12dp"
            android:layout_marginEnd="8dp"
            app:layout_constraintStart_toEndOf="@id/cardWorkoutIcon"
            app:layout_constraintEnd_toStartOf="@id/cbWorkoutCompleted"
            app:layout_constraintTop_toTopOf="parent" />

        <!-- Workout Date -->
        <TextView
            android:id="@+id/tvWorkoutDate"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="12dp"
            android:layout_marginTop="2dp"
            android:layout_marginEnd="8dp"
            android:text="Datum"
            android:textSize="13sp"
            android:textColor="@color/md_theme_light_onSurfaceVariant"
            android:maxLines="1"
            android:ellipsize="end"
            app:layout_constraintStart_toEndOf="@id/cardWorkoutIcon"
            app:layout_constraintEnd_toStartOf="@id/cbWorkoutCompleted"
            app:layout_constraintTop_toBottomOf="@id/tvWorkoutName" />

        <!-- Chips Container -->
        <LinearLayout
            android:id="@+id/layoutChips"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginTop="6dp"
            android:layout_marginStart="12dp"
            android:layout_marginEnd="8dp"
            android:orientation="horizontal"
            app:layout_constraintStart_toEndOf="@id/cardWorkoutIcon"
            app:layout_constraintEnd_toStartOf="@id/cbWorkoutCompleted"
            app:layout_constraintTop_toBottomOf="@id/tvWorkoutDate">

            <com.google.android.material.chip.Chip
                android:id="@+id/chipDuration"
                android:layout_width="wrap_content"
                android:layout_height="28dp"
                android:text="30 min"
                android:textSize="11sp"
                android:textColor="@color/md_theme_light_onSecondaryContainer"
                app:chipBackgroundColor="@color/md_theme_light_secondaryContainer"
                app:chipCornerRadius="14dp"
                app:chipMinHeight="28dp"
                app:chipStartPadding="8dp"
                app:chipEndPadding="8dp"
                app:chipStrokeWidth="0dp"
                app:ensureMinTouchTargetSize="false"
                style="@style/Widget.Material3.Chip.Assist" />

            <com.google.android.material.chip.Chip
                android:id="@+id/chipIntensity"
                android:layout_width="wrap_content"
                android:layout_height="28dp"
                android:layout_marginStart="6dp"
                android:text="Střední"
                android:textSize="11sp"
                android:textColor="@android:color/white"
                app:chipBackgroundColor="@color/md_theme_light_tertiary"
                app:chipCornerRadius="14dp"
                app:chipMinHeight="28dp"
                app:chipStartPadding="8dp"
                app:chipEndPadding="8dp"
                app:chipStrokeWidth="0dp"
                app:ensureMinTouchTargetSize="false"
                style="@style/Widget.Material3.Chip.Assist" />
        </LinearLayout>

        <!-- Checkbox -->
        <CheckBox
            android:id="@+id/cbWorkoutCompleted"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:buttonTint="@color/md_theme_light_tertiary"
            app:layout_constraintEnd_toStartOf="@id/btnDeleteWorkout"
            app:layout_constraintTop_toTopOf="parent" />

        <!-- Delete Button -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnDeleteWorkout"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:insetTop="0dp"
            android:insetBottom="0dp"
            android:insetLeft="0dp"
            android:insetRight="0dp"
            android:padding="0dp"
            app:icon="@android:drawable/ic_menu_delete"
            app:iconGravity="textStart"
            app:iconPadding="0dp"
            app:iconSize="20dp"
            app:iconTint="@color/md_theme_light_error"
            app:cornerRadius="20dp"
            app:strokeWidth="1dp"
            app:strokeColor="@color/md_theme_light_error"
            app:backgroundTint="@color/md_theme_light_errorContainer"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            style="@style/Widget.Material3.Button.OutlinedButton" />

    </androidx.constraintlayout.widget.ConstraintLayout>

</com.google.android.material.card.MaterialCardView>